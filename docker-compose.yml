services:
  quotanoa-client:
    image: ghcr.io/leisurelyyrsc/quotanoa-client:latest
    pull_policy: always
    restart: unless-stopped
    init: true
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
